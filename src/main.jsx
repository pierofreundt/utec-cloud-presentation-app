import React from "react"
import ReactDOM from "react-dom/client"
import ProyectoParcial from "./components/ProyectoParcial"

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ProyectoParcial />
  </React.StrictMode>
)
